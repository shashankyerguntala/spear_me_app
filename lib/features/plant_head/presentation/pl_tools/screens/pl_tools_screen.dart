import 'package:flutter/material.dart';

class PlToolsScreen extends StatelessWidget {
  const PlToolsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
