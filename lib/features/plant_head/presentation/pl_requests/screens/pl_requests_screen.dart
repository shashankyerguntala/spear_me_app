import 'package:flutter/material.dart';

class PlRequestsScreen extends StatelessWidget {
  const PlRequestsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}