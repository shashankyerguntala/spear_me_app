import 'package:flutter/material.dart';

class PlProfileScreen extends StatelessWidget {
  const PlProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}